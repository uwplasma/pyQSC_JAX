# First-order QH

The QH example has nonzero Frenet-frame helicity. Consequently `iota` and
`iotaN` differ by an integer multiple of the field-period count.

```{literalinclude} ../../examples/02_first_order_qh.py
:language: python
:linenos:
```

The printed helicity is a topology diagnostic computed from the magnetic
axis, not a user-supplied QA/QH label.
